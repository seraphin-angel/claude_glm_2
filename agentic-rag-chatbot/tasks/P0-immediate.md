# P0 - 即時対応（10件）

> 基盤の安全性確保。セキュリティ脆弱性と致命的バグの修正。
> 目標期間: 即時〜1ヶ月

## 進捗サマリー

| 完了 | 進行中 | 未着手 | ブロック | 進捗率 |
|------|--------|--------|----------|--------|
| 0 | 0 | 10 | 0 | 0% |

---

## タスク一覧

### 1. `quality.py` フォールバック修正（`passed=False`）

| 項目 | 内容 |
|------|------|
| ID | P0-01 |
| カテゴリ | RAG |
| 難易度 | 低 |
| 対象ファイル | `backend/app/tools/quality.py` |
| ステータス | `[ ]` 未着手 |
| 着手日 | - |
| 完了日 | - |
| 担当 | - |
| 関連PR | - |

**概要:** `check_quality` ツールの JSON パースエラー時に `passed=True`（合格）をデフォルト返却しており、品質チェックが実質的にバイパスされる。`passed=False` に変更する。

**受け入れ条件:**
- [ ] `except (json.JSONDecodeError, IndexError)` ブロックで `passed=False` を返すように修正
- [ ] `hallucination_score` を安全方向のデフォルト値に変更
- [ ] ユニットテストでエラー時のフォールバック動作を検証

**備考:**
```
```

---

### 2. レート制限の実装

| 項目 | 内容 |
|------|------|
| ID | P0-02 |
| カテゴリ | セキュリティ |
| 難易度 | 低 |
| 対象ファイル | `backend/app/api/`, `backend/app/main.py` |
| ステータス | `[ ]` 未着手 |
| 着手日 | - |
| 完了日 | - |
| 担当 | - |
| 関連PR | - |

**概要:** API エンドポイントにレート制限がなく、大量リクエストによる DoS 攻撃や OpenAI API コスト爆発のリスクがある。`slowapi` によるレート制限を実装する。

**受け入れ条件:**
- [ ] `slowapi` をインストールし、Limiter を設定
- [ ] チャット開始エンドポイントに `10/minute` のレート制限を設定
- [ ] SSE ストリームエンドポイントにもレート制限を設定
- [ ] レート制限超過時に適切な 429 レスポンスを返す
- [ ] テストでレート制限動作を検証

**備考:**
```
```

---

### 3. エラーメッセージの内部情報除去

| 項目 | 内容 |
|------|------|
| ID | P0-03 |
| カテゴリ | セキュリティ |
| 難易度 | 低 |
| 対象ファイル | `backend/app/services/chat_service.py` |
| ステータス | `[ ]` 未着手 |
| 着手日 | - |
| 完了日 | - |
| 担当 | - |
| 関連PR | - |

**概要:** 例外のスタックトレースをそのまま SSE でユーザーに送信しており、API キー情報、ファイルパス、内部構造が漏洩するリスクがある。

**受け入れ条件:**
- [ ] エラー詳細をサーバーログに記録（`logger.error` + `exc_info=True`）
- [ ] ユーザー向けには汎用的なエラーメッセージのみ返す
- [ ] 環境変数でデバッグモード切り替え可能にする（開発時はスタックトレース表示）
- [ ] テストでエラー時のレスポンスに内部情報が含まれないことを検証

**備考:**
```
```

---

### 4. SSE キューのメモリリーク修正

| 項目 | 内容 |
|------|------|
| ID | P0-04 |
| カテゴリ | セキュリティ |
| 難易度 | 低 |
| 対象ファイル | `backend/app/services/chat_service.py` |
| ステータス | `[ ]` 未着手 |
| 着手日 | - |
| 完了日 | - |
| 担当 | - |
| 関連PR | - |

**概要:** `ChatService._queues` と `_tasks` にサイズ制限がなく、`cleanup()` が API から呼ばれない。SSE 切断後もキューが残留し続け、メモリ枯渇のリスクがある。

**受け入れ条件:**
- [ ] `_queues` の最大同時接続数を制限（例: 1000）
- [ ] `Queue` の `maxsize` を設定（例: 100）
- [ ] SSE 終了時に `finally` ブロックで `cleanup()` を確実に呼び出す
- [ ] 古いキューの自動退去（eviction）メカニズムを実装
- [ ] テストでクリーンアップ動作を検証

**備考:**
```
```

---

### 5. thread_id UUID バリデーション

| 項目 | 内容 |
|------|------|
| ID | P0-05 |
| カテゴリ | セキュリティ |
| 難易度 | 低 |
| 対象ファイル | `backend/app/api/` |
| ステータス | `[ ]` 未着手 |
| 着手日 | - |
| 完了日 | - |
| 担当 | - |
| 関連PR | - |

**概要:** `thread_id` にフォーマットバリデーションがなく、任意文字列が通過する。UUID 形式を強制する。

**受け入れ条件:**
- [ ] `thread_id` パラメータに `UUID4` 型バリデーションを追加
- [ ] 存在しない `thread_id` に対して 404 を返す
- [ ] 不正形式の `thread_id` に対して 422 を返す
- [ ] テストで各バリデーションケースを検証

**備考:**
```
```

---

### 6. LLM インスタンスのシングルトン化

| 項目 | 内容 |
|------|------|
| ID | P0-06 |
| カテゴリ | RAG |
| 難易度 | 低 |
| 対象ファイル | `backend/app/tools/` 各ツールファイル |
| ステータス | `[ ]` 未着手 |
| 着手日 | - |
| 完了日 | - |
| 担当 | - |
| 関連PR | - |

**概要:** 各ツール（`classify.py`, `rewrite.py`, `relevance.py`, `generate.py`, `quality.py`）が毎回 `ChatOpenAI` インスタンスを生成している。共通のファクトリ関数でシングルトン化する。

**受け入れ条件:**
- [ ] `@lru_cache` を使用した LLM ファクトリ関数を作成（temperature 別に2種類）
- [ ] 全ツールファイルを共通ファクトリ関数に移行
- [ ] テストで同一インスタンスが返されることを検証

**備考:**
```
```

---

### 7. StructuredOutput 導入

| 項目 | 内容 |
|------|------|
| ID | P0-07 |
| カテゴリ | RAG |
| 難易度 | 低 |
| 対象ファイル | `backend/app/tools/classify.py`, `rewrite.py`, `relevance.py`, `quality.py` |
| ステータス | `[ ]` 未着手 |
| 着手日 | - |
| 完了日 | - |
| 担当 | - |
| 関連PR | - |

**概要:** 全ツールが LLM レスポンスを手動で JSON パースしている。`with_structured_output()` と Pydantic モデルを導入して型安全性を向上させる。

**受け入れ条件:**
- [ ] 各ツールの出力を Pydantic モデルとして定義
- [ ] `llm.with_structured_output(Model)` に移行
- [ ] 手動 JSON パース処理を削除
- [ ] フォールバック処理を型安全なデフォルト値に変更
- [ ] テストで型安全な出力を検証

**依存:** P0-06（LLM シングルトン化）と合わせて実装すると効率的

**備考:**
```
```

---

### 8. エージェント反復回数制限

| 項目 | 内容 |
|------|------|
| ID | P0-08 |
| カテゴリ | RAG |
| 難易度 | 低 |
| 対象ファイル | `backend/app/agent/`, `backend/app/prompts.py` |
| ステータス | `[ ]` 未着手 |
| 着手日 | - |
| 完了日 | - |
| 担当 | - |
| 関連PR | - |

**概要:** `create_react_agent` に反復回数制限がなく、品質チェック失敗のループで無限に LLM を呼び出す可能性がある。

**受け入れ条件:**
- [ ] `astream_events` ラッパーでステップ数を監視（MAX_STEPS=10）
- [ ] プロンプトに `check_quality` 不合格時の最大リトライ回数（2回）を明記
- [ ] 3回目以降は `ask_human` でエスカレーション指示をプロンプトに追加
- [ ] テストで反復制限超過時の動作を検証

**備考:**
```
```

---

### 9. プロンプトインジェクション対策

| 項目 | 内容 |
|------|------|
| ID | P0-09 |
| カテゴリ | セキュリティ |
| 難易度 | 中 |
| 対象ファイル | `backend/app/tools/` 全ツール, `backend/app/services/` |
| ステータス | `[ ]` 未着手 |
| 着手日 | - |
| 完了日 | - |
| 担当 | - |
| 関連PR | - |

**概要:** 全ツールがユーザー入力を f-string でプロンプトに直接展開しており、プロンプトインジェクション攻撃に脆弱。

**受け入れ条件:**
- [ ] ユーザー入力を `HumanMessage` として構造化メッセージで渡すように変更
- [ ] 基本的なインジェクションパターン検出関数を実装
- [ ] 検出時にログ記録 + 安全な応答を返す
- [ ] テストで代表的なインジェクションパターンをテスト

**備考:**
```
```

---

### 10. JWT 認証の実装

| 項目 | 内容 |
|------|------|
| ID | P0-10 |
| カテゴリ | セキュリティ |
| 難易度 | 中 |
| 対象ファイル | `backend/app/api/`, `backend/app/main.py`, 新規: `backend/app/auth/` |
| ステータス | `[ ]` 未着手 |
| 着手日 | - |
| 完了日 | - |
| 担当 | - |
| 関連PR | - |

**概要:** 全 API エンドポイントに認証が一切なく、誰でもチャットを利用でき、OpenAI API コストを無制限に消費させることが可能。JWT ベースの認証を実装する。

**受け入れ条件:**
- [ ] `HTTPBearer` + JWT トークン検証ミドルウェアを実装
- [ ] 全エンドポイントに認証を適用（`Depends(verify_token)`）
- [ ] SSE エンドポイントにもトークンベースの認証を適用
- [ ] トークン発行 / 検証のユーティリティ関数を作成
- [ ] 認証エラー時に適切な 401/403 レスポンスを返す
- [ ] テストで認証あり/なしの動作を検証

**依存:** P0-02（レート制限）と組み合わせると効果的

**備考:**
```
```

---

## 依存関係マップ

```
P0-06 (LLMシングルトン) ──→ P0-07 (StructuredOutput) ※合わせて実施が効率的
P0-02 (レート制限)     ──→ P0-10 (JWT認証)          ※組み合わせ推奨
P0-01 (品質フォールバック) → P0-07 (StructuredOutput) ※P0-07で根本解決
```
