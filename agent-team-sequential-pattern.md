# Agent Teams: 逐次サブエージェント使い捨てパターン

## 概要

Claude Code の Agent Teams において、**タスクごとにサブエージェントを新規起動し、完了後に終了させる**パターン。
コンテキストウィンドウの肥大化を防ぎ、各タスクをフレッシュな状態で処理できる。

## アーキテクチャ

```
┌─ Leader（親エージェント）──────────────────────────────┐
│  ずっと生存。タスク管理・指示・結果の受領              │
│                                                       │
│  タスク1 → Sub-Agent-1 を起動                         │
│            ↓ 完了報告                                  │
│            ↓ shutdown_request で終了 → コンテキスト破棄 │
│                                                       │
│  タスク2 → Sub-Agent-2 を新規起動（フレッシュ）        │
│            ↓ 完了報告                                  │
│            ↓ shutdown_request で終了 → コンテキスト破棄 │
│                                                       │
│  タスク3 → Sub-Agent-3 を新規起動（フレッシュ）        │
│            ...                                        │
└───────────────────────────────────────────────────────┘
```

## 実行フロー

```
loop:
  task = 次の未完了タスク（blockedBy が空のもの）
  if task == null: break

  // サブエージェント起動
  agent = Task(
    subagent_type: "general-purpose",
    model: "sonnet",
    team_name: current_team,
    prompt: build_prompt(task)
  )

  // 完了待ち → 結果受領

  // サブエージェント終了
  SendMessage(type: "shutdown_request", recipient: agent)

  // タスク完了記録
  TaskUpdate(taskId: task.id, status: "completed")

  // 成果物情報を記録（次タスクへの引き継ぎ用）
  記録: task の成果ファイルパス、変更内容サマリー
```

---

## 使い方（以下から1つ選ぶ）

以下の3つは**独立した選択肢**です。どれか1つを選んで使ってください。

```
選択肢A・B（毎回プロンプトに書く）:
┌──────────────────────────────────┐
│ claude "TODOアプリを作って。     │
│                                  │
│ 【実行ルール】                    │
│ - 1タスクごとにサブエージェント... │ ← 毎回これを書く
│ - 使い回し禁止...                │
│ - ..."                           │
└──────────────────────────────────┘

選択肢C（ファイルに保存して再利用）:
┌─ .claude/agents/sequential-leader.md ─┐
│ 実行ルールが事前に書いてある           │ ← 一度作れば永続
└────────────────────────────────────────┘
        ↓ プロンプトでは要件だけ書けばOK
┌──────────────────────────────────┐
│ claude "TODOアプリを作って"       │ ← ルールを書かなくていい
└──────────────────────────────────┘
```

| 選択肢 | 特徴 | 向いているケース |
|--------|------|-----------------|
| **A: 詳細プロンプト** | プロンプトに戦略を全部書く（詳細版） | 初めて試す時、細かく制御したい時 |
| **B: 簡易プロンプト** | プロンプトに戦略を全部書く（簡易版） | 慣れてきて最低限の指示で済ませたい時 |
| **C: エージェント定義ファイル** | 戦略をファイルに保存し、プロンプトは要件のみ | 何度も同じパターンを使う時（推奨） |

---

### 選択肢A: 詳細プロンプト（毎回書く）

```
以下のアプリケーションを構築してください。

## 構築内容
[ここにアプリの要件を書く]

## タスク分割と実行戦略（必ず従うこと）

あなたはリーダーとして以下のワークフローに厳密に従ってください：

1. まず全体を独立した小タスクに分割し、TaskCreate で全タスクを登録する
2. タスクは1つずつ順番に処理する
3. 各タスクの処理フロー：
   a. 新しいサブエージェントを Task ツールで起動する
   b. そのサブエージェントに「タスクの内容」「関連ファイルパス」
      「前のタスクの成果物の情報」を詳細にプロンプトで渡す
   c. サブエージェントの完了報告を待つ
   d. 完了したら SendMessage(type: "shutdown_request") で
      サブエージェントを終了させる
   e. TaskUpdate でタスクを completed にする
4. 次のタスクで新しいサブエージェントを起動する（前のものは再利用しない）
5. 全タスク完了後、最終確認を行う

## 重要なルール
- サブエージェントは1タスクにつき1インスタンス（使い回さない）
- タスク完了後は必ず shutdown_request でサブエージェントを終了させる
- 次のサブエージェントには、前タスクの結果として必要な情報
  （作成されたファイルパス、API 仕様など）を必ずプロンプトに含める
- あなた自身（リーダー）はコードを書かない。指示と管理に専念する
```

---

### 選択肢B: 簡易プロンプト（毎回書く）

```
[要件をここに書く]

【実行ルール】
- チームを作り、全タスクを TaskCreate で登録
- 1タスクごとに新しいサブエージェントを起動
- 完了したらそのサブエージェントを shutdown して次へ
- サブエージェントの再利用禁止
- あなた（リーダー）はコードを書かず管理に専念
- 次のサブエージェントには前タスクの成果ファイルパスを必ず伝える
```

---

### 選択肢C: エージェント定義ファイル（一度作れば再利用可能）

以下の内容を `.claude/agents/sequential-leader.md` として保存する。
保存後は、プロンプトに要件だけ書けばこの戦略が自動的に適用される。

```markdown
あなたは「逐次タスク実行リーダー」です。

## あなたの役割
- タスクの分割と計画
- サブエージェントへの指示出し
- 進捗管理と品質確認
- あなた自身はコードを書かない

## 実行プロトコル

### Phase 1: 計画
1. ユーザーの要件を分析する
2. 独立した小タスク（1タスク = 1機能 or 1ファイル程度）に分割
3. TaskCreate で全タスクを登録
4. 依存関係があれば TaskUpdate で blockedBy を設定
5. タスク一覧をユーザーに提示し、承認を得る

### Phase 2: 逐次実行
各タスクについて以下を繰り返す：

1. 新しいサブエージェントを Task ツールで起動
   - subagent_type: "general-purpose"
   - model: "sonnet"
2. プロンプトに以下を含める：
   - タスクの具体的な内容と完了条件
   - 対象ファイルパス
   - 前のタスクで作成・変更されたファイル一覧
   - プロジェクトの技術スタック
   - コーディング規約（あれば）
3. 完了報告を待つ
4. SendMessage(type: "shutdown_request") で終了
5. TaskUpdate で completed にする
6. 成果物情報を記録し、次のタスクへ

### Phase 3: 最終確認
全タスク完了後：
1. 全体のビルド確認
2. テスト実行
3. ユーザーへの完了報告

## 厳守ルール
- サブエージェントは1タスク1インスタンス（再利用しない）
- タスク完了後は必ず shutdown_request で終了させる
- リーダー自身はコードを書かない
- 次のサブエージェントには前タスクの成果物情報を必ず渡す
```

#### 選択肢C の使い方

```bash
# ファイルを保存した後は、プロンプトに要件だけ書けばOK
claude "TODOアプリを作ってください。React + Express + SQLite 構成で、
CRUD操作とユーザー認証を含む。"
```

---

## 効果的なフレーズ集

リーダーの振る舞いを制御する上で効きやすい表現（選択肢A・Bで使う場合に参考）：

| 目的 | 効果的なフレーズ |
|------|----------------|
| 使い回し防止 | 「サブエージェントは**1タスク1インスタンス**。再利用しない」 |
| 終了の徹底 | 「タスク完了後は**必ず** shutdown_request で終了させる」 |
| リーダーの役割限定 | 「あなた自身は**コードを書かない**。指示と管理に専念する」 |
| 引き継ぎの徹底 | 「次のサブエージェントには**前タスクの成果物情報を必ずプロンプトに含める**」 |
| 粒度の指定 | 「1タスク = **1つの機能 or 1つのファイル**程度の粒度」 |
| 順序の強制 | 「タスクは**1つずつ逐次処理**する。並列実行しない」 |

---

## 利点と注意点

### 利点

| 利点 | 説明 |
|------|------|
| コンテキスト管理 | 各サブエージェントがフレッシュな状態で開始 |
| 品質維持 | コンテキスト末期の精度低下を回避 |
| 独立性 | タスク間の情報汚染がない |
| 共有タスクリスト | `~/.claude/tasks/{team-name}/` でタスク状態を一元管理 |

### 注意点

- **リーダー自身のコンテキストは蓄積し続ける**（ただしコード詳細は含まないため軽い）
- **引き継ぎ情報が重要**: Sub-Agent-2 は Sub-Agent-1 のコンテキストを持たないため、リーダーが成果物情報をプロンプトに含める必要がある
- Claude Code の自動コンテキスト圧縮機能により、リーダー側もある程度は対処される

### 並列 vs 逐次

```
逐次（本パターン）:
  Sub-1 → 完了 → 終了 → Sub-2 → 完了 → 終了 → Sub-3

並列（独立タスクの場合）:
  Sub-1 ─────→ 完了 → 終了
  Sub-2 ─────→ 完了 → 終了    ← 同時に動作
  Sub-3 ─────→ 完了 → 終了
```

独立したタスクなら並列も可能。タスクの依存関係に応じて使い分ける。
